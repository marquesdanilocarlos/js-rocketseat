//Buscar só um elemento pelo id
const guest = document.querySelector('#guest-2');
console.log(guest);

//Buscar vários elementos pela classe
const guestsQuerySelector = document.querySelectorAll('.guest');
console.log(guestsQuerySelector);